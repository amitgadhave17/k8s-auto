apiVersion: v1
kind: Pod
metadata:
  name: cryptominer-execution
spec:
  restartPolicy: Never
  containers:
  - name: cryptominer-execution
    image: angelrita/base-ubuntu:latest
    #imagePullPolicy: Never
    command: ['sh']
    args: ['-c', 'modprobe msr allow_writes=on']
